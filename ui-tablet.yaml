# https://community.simon42.com/t/wall-tablet-dashboard-breakdown/31279/36
title: Home
views: !include_dir_list "/config/ui_lovelace_tablet/views/"
button_card_templates: !include_dir_merge_named "/config/ui_lovelace_tablet/button_card_templates/"

kiosk_mode:
  non_admin_settings:
    hide_header: |
      [[[
        return states["input_boolean.debug_mode"].state === "on" ? false : true;
      ]]]
    ignore_entity_settings: |
      [[[
        return states["input_boolean.debug_mode"].state === "on" ? false : true;
      ]]]
    hide_sidebar: |
      [[[
        return states["input_boolean.debug_mode"].state === "on" ? false : true;
      ]]]
  admin_settings:
    hide_header: |
      [[[
        return states["input_boolean.debug_mode"].state === "on" ? false : true;
      ]]]
    ignore_entity_settings: |
      [[[
        return states["input_boolean.debug_mode"].state === "on" ? false : true;
      ]]]
    hide_sidebar: |
      [[[
        return states["input_boolean.debug_mode"].state === "on" ? false : true;
      ]]]